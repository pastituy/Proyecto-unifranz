const express = require("express");
const { PrismaClient } = require("@prisma/client");

const app = express.Router();
const prisma = new PrismaClient();

// ================================
// RUTAS TRABAJADOR SOCIAL
// En reestructuración
// ================================

module.exports = app;
